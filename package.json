{
  "name": "projectflow",
  "version": "1.0.0",
  "description": "MERN Project Management App",
  "scripts": {
    "build": "npm ci --prefix client && npm run build --prefix client",
    "install-client": "npm ci --prefix client"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "morgan": "^1.10.0"
  }
}
